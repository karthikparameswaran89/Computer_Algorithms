def splitArray(arr: List[Int], n: Int): List[List[Int]] = {
  val sortedArr = arr.sorted.reverse
  val subarrays = Array.fill[List[Int]](n)(List.empty)
  sortedArr.foreach { x =>
    val idx = subarrays.zipWithIndex.minBy(_._1.sum)._2
    subarrays(idx) ::= x
  }
  subarrays.toList
}
